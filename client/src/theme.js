import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#238636",
    },
    background: {
      default: "#0d1117",
      paper: "#161b22",
    },
  },
});

export default theme;
